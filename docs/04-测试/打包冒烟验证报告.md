# 打包冒烟验证报告 —— o32-ops-platform v0.1.0（2026-07-17）

## 1. 验证对象与方法

| 项 | 内容 |
|---|---|
| 构建 | `server\packaging\build.spec`（PyInstaller 6.21.0，onedir / console / 无 UPX） |
| 产物 | `package\o32-ops-platform-v0.1.0\`（111 MB）+ `o32-ops-platform-v0.1.0.zip`（47.7 MB） |
| 方法 | zip 解压到**独立目录** `D:\ITOps\smoke-test\`（脱离源码与 server\.venv），直接运行 `app\o32-server.exe --port 8017`，curl 完成全流程冒烟 |
| 局限 | 本机无干净 Windows 虚拟机，未在"零运行时"系统上复验；构建机自身装有 Python（但冒烟进程未使用 venv，exe 内嵌运行时独立运行）——干净虚拟机验证待补（验收 V10 差距，见第 5 节） |

## 2. 冒烟全流程（命令与关键输出）

启动：`o32-server.exe --port 8017` → 横幅打印服务地址/数据目录 →
日志显示 `数据库初始化完成 … 规则导入: 代码映射 21 条 / 大宗产品 11 个 / 阈值 diff_pct / fuzzy_sim / price_tol(预留)`。

| 步骤 | 命令（要点） | 关键输出 | 结论 |
|---|---|---|---|
| 1 健康检查 | `GET /api/health` | `{"status":"ok",…}` | ✅ |
| 2 admin 登录 | `POST /api/auth/login` | 返回 token，`must_change_password=true` | ✅ |
| 3 强制改密 | `POST /api/auth/change-password` | `密码修改成功` | ✅ |
| 4 建操作员 | `POST /api/admin/users`（op01） | 200，op01 登录改密完成 | ✅ |
| 5 M1 任务 | `POST /api/recon/m1/jobs`（黄金样本两表） | `job_id=3f76c332611141b2` → 轮询 success | ✅ |
| 6 M1 统计 | `GET /api/recon/jobs/{id}` | `16/14/1/1/2/1`，与黄金基线 **逐项一致** | ✅ |
| 7 M1 下载+颜色 | `GET …/download` | 200，7189 字节；表头 B4C7DC、第2/12行大宗 FFC000、第3行差异 FFCCCC、第17行未匹配 FFFF99、共17行 | ✅ |
| 8 M3 任务 | `POST /api/recon/m3/jobs`（M3 样本） | `job_id=de4d51e4be914066` → success | ✅ |
| 9 M3 统计 | `GET /api/recon/jobs/{id}` | `15/有变动5/无变化4/未匹配6`，与 M3 基线一致；result_files 三件套+run.log | ✅ |
| 10 M3 三件套 | `GET …/download?file=updated\|detail\|note` | 三件均 200（5716/5532/1492 字节） | ✅ |
| 11 规则读写 | `GET /api/rules/mappings` 21 条、`bulk-products` 11 个；`PUT fuzzy_sim=0.6` 生效并恢复 0.5；`GET /api/rules/export` | 导出 rename_map / bulk_products 与基准 config **逐项一致**，阈值 1.0/0.5 | ✅ |
| 12 关闭 | `taskkill o32-server.exe` | 8017 无监听 | ✅ |

## 3. 数据库驱动打包验证（二期免安装可行性）

四个驱动均安装成功并随包收集（`collect_all`）。验证方式：以 `_internal` 优先
于 venv 加载并导入，确认实际从部署包内解析（非构建机环境）：

| 驱动 | 版本 | 免安装形态 | 包内导入验证 |
|---|---|---|---|
| oracledb | 4.0.2 | thin 模式纯 Python（含 thin_impl.pyd，无需 Oracle Client） | ✅ 从包内导入成功 |
| PyMySQL | 1.2.0 | 纯 Python | ✅ 从包内导入成功 |
| psycopg2-binary | 2.9.12 | 自带 libpq（_psycopg.pyd + psycopg2_binary.libs） | ✅ 从包内导入成功 |
| pymssql | 2.3.13 | 自带 FreeTDS（_mssql.pyd） | ✅ 从包内导入成功 |

结论：四驱动二进制级"随包免安装"成立；真实数据库连接验证待二期具备测试库后补充。

## 4. 五门禁复核（打包完成后源码环境重跑）

| 门禁 | 结果 |
|---|---|
| M1 黄金回归 | 21 PASS / 0 FAIL，EXIT=0 ✅ |
| M3 黄金回归 | 33 PASS / 0 FAIL，EXIT=0 ✅ |
| M1 API 冒烟 | 54 PASS / 0 FAIL，EXIT=0 ✅ |
| M3 API 冒烟 | 41 PASS / 0 FAIL，EXIT=0 ✅ |
| 规则配置冒烟 | 109 PASS / 0 FAIL，EXIT=0 ✅ |

## 5. 与验收 V10 的差距

| V10 要求 | 本步状态 | 差距/待办 |
|---|---|---|
| 无任何运行时的干净 Windows 环境解压即用 | exe 在独立目录（脱离源码与 venv）全流程跑通 | **未在干净虚拟机复验**：构建机装有 Python，虽冒烟进程未使用之，仍建议发布前在一台干净 Windows 虚拟机按《部署手册》复走一遍（重点：首启建库、端口、防火墙、install.bat 服务注册） |
| 服务化（install/uninstall） | install.bat/uninstall.bat 已提供（NSSM 优先 + 计划任务回退） | 本打包机会话**无管理员权限**，服务注册未实机执行；NSSM 在本机非交互会话挂起未能验证（疑终端安全软件沙箱），已附 SHA-256 与自检说明，目标服务器需管理员先验 nssm 可用性 |
| 冒烟验证记录 | 本报告 | 干净虚拟机冒烟截图/记录待补 |

## 6. 结论

部署包在独立目录全流程冒烟通过（健康/认证/M1/M3/规则/下载/颜色抽样全部符合基线），
四数据库驱动包内可导入，源码侧五门禁无回归。具备发布试运行条件；
按第 5 节补齐干净虚拟机验证后即满足验收 V10。

---

# 附：v0.4.0 打包冒烟验证（2026-07-20，M2 估值价格核对）

## A. 验证对象与方法

| 项 | 内容 |
|---|---|
| 构建 | `server\packaging\build.bat`（VERSION=0.4.0；前端 vite build → PyInstaller onedir → 组装 → Compress-Archive） |
| 产物 | `package\o32-ops-platform-v0.4.0\`（113 MB，950 条目）+ `o32-ops-platform-v0.4.0.zip`（50.7 MB） |
| 方法 | zip 解压到**独立目录** `D:\ITOps\smoke-test4\`（脱离源码与 server\.venv），运行 `app\o32-server.exe --port 8020`，脚本化全流程冒烟（tests/live_check_m2.py，BASE 指向 8020） |
| 局限 | 同 v0.1.0：未在干净 Windows 虚拟机复验（验收 V10 差距仍待补） |

## B. 冒烟全流程（13 项断言，全部通过）

| 步骤 | 关键输出 | 结论 |
|---|---|---|
| 1 健康检查 | `{"status":"ok","service":"o32-ops-platform","phase":"二期(M1+M2+M3)"}` | ✅ |
| 2 登录改密 | admin 首登改密 200 | ✅ |
| 3 种子规则 | GET subject-price-rules：1101→市价、1501→单位成本（打包版首启自动预置） | ✅ |
| 4 M2 基线任务 | 4 黄金样本文件上传 → 轮询 success | ✅ |
| 5 基线统计 | 6301: 7/2/2/3；6302: 2/1/1/0；合计 9/3/3/3，与黄金基线**逐项一致** | ✅ |
| 6 按产品下载 | `?product=6301` 200（PK 头） | ✅ |
| 7 热生效（加规则） | 新增 1102→市价（免重启）→ 重跑 6301=**8/2/2/4**、合计 10/3/3/4 | ✅ |
| 8 热生效（删规则） | DELETE 1102 → 重跑回到基线 7/2/2/3 | ✅ |
| 9 SPA 路由 | `/recon/m2`、`/system/config` 均 200 html | ✅ |
| 10 关闭 | taskkill 后 8020 无监听 | ✅ |

前端构建：`npm run build` 通过，ReconM2View / SystemConfigView 入包；
无头 Chrome 渲染截图核验两新页面结构与菜单挂载正确（源码态 8019 同流程另验）。

## C. 打包脚本修复记录

- `build.bat` 版本号 0.3.0→0.4.0 并统一 CRLF（LF 会导致 cmd 解析失败，一期已知坑）；
- 尾部 `echo 构建完成/目录/压缩` 中文行在 chcp 65001 + setlocal 场景触发 cmd
  多字节解析怪癖（'目录' is not recognized，仅影响显示不影响产物与退出码），
  已改为纯 ASCII 输出并实测干净；
- `gen_pkg_info.py` CHANGELOG 增补 v0.4.0 条目（版本.txt 随包生成）。

## D. 门禁复核（v0.4.0 打包完成后源码环境重跑）

十门禁 470 项断言全部 PASS / 0 FAIL（明细见《M2估值价格核对测试报告》§1）：
M1/M2/M3 黄金回归 21/26/33；M1/M2/M3 API 冒烟 54/67/41；
规则 109；数据源 76；SQL Guard 75；DbAdapter 28。


---

# 附录 C：v0.5.0 打包冒烟验证（2026-07-20）

## A. 产物

| 项 | 值 |
|---|---|
| 包目录 | `package\o32-ops-platform-v0.5.0\`（114MB） |
| 压缩包 | `package\o32-ops-platform-v0.5.0.zip`（49MB） |
| 打包配置变更 | `build.bat` VERSION 0.4.0→0.5.0（CRLF 保持）；`build.spec` 增 `collect_all('apscheduler')`（jobstores/triggers/executors 字符串引用动态加载）；`requirements-lock.txt` 重新 freeze（APScheduler==3.11.3、tzlocal==5.4.4）；`gen_pkg_info.py` CHANGELOG 增 v0.5.0 条目与 License 标注 |
| 冒烟目录 | `D:\ITOps\smoke-test5\`（zip 解压即用，`app\o32-server.exe --port 8021`） |

## B. 冒烟步骤与结果（22 项断言全 PASS）

打包态服务首启正常：建库建表（含 schedule_job / recon_job_item / sys_config 新表）、
种子导入、**APScheduler 随 exe 启动并恢复调度**（jobstore 持久化到平台库）。

| 步骤 | 关键输出 | 结论 |
|---|---|---|
| 1 健康检查 | `{"status":"ok",...}` 200 | ✅ |
| 2 前端首页 | GET / 返回 SPA HTML（含挂载点） | ✅ |
| 3 未认证拦截 | 受保护接口 401 | ✅ |
| 4 登录改密 | admin 首登改密 200；建 operator 200 | ✅ |
| 5 M1 文件模式 | 黄金样本上传 → success，统计==基线 16/14/1/1/2/1，结果下载 200 | ✅ |
| 6 M1 db 模式 | sqlite 数据源 + 双模板 → fetch_mode=db → success，统计==基线；查询快照 CSV 落归档（UTF-8-SIG 带 BOM） | ✅ |
| 7 调度链路 | 创建 db 模式定时任务 200 → run-now → last_status=success；recon_job `trigger_type=schedule` 且统计==基线 | ✅ |
| 8 看板接口 | diff-trend 当日点==基线（16/14/差异1）；health：schedule 1/1、近30天成功≥1、recent 非空；persistent-diff 200 | ✅ |
| 9 前端渲染 | 无头 Chrome 截图：登录页渲染正常（exe 托管 SPA） | ✅ |

## C. 门禁复核（v0.5.0 打包完成后源码环境重跑）

12 组门禁 **710 项断言全部 PASS / 0 FAIL**：M1/M2/M3 黄金回归 21/26/33；
M1/M2/M3 API 冒烟 54/67/41；规则 109；数据源 76；SQL Guard 75；DbAdapter 28；
取数模式 70；调度+看板 80；调度补测（file/M2）30。
明细见《取数联动与调度看板测试报告》§3。
